# Supabase sync (spec §11). Safe to ship in a static build — Row Level
# Security restricts every row to its owner. Copy to .env and fill in from
# your Supabase project's Settings → API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
